import { makeStyles } from "@material-ui/styles";

const style = {

  /**
   * @todo Add styles for FormLabel
   */
  root: {

  },
};

export const useStyles = makeStyles(style, { name: "FacioFormLabel" });

export default useStyles;
