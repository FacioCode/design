import * as React from "react";
import { default as MaterialScopedCssBaseline } from "@material-ui/core/ScopedCssBaseline";

export const ScopedCssBaseline = MaterialScopedCssBaseline;
export default ScopedCssBaseline;
